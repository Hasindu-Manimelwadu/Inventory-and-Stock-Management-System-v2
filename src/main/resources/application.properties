server.port=8084

# JSP Configuration
spring.mvc.view.prefix=/WEB-INF/views/
spring.mvc.view.suffix=.jsp

# Application name
spring.application.name=SupplierManagement

# File storage path (data stored in text files as per project requirement)
app.data.path=data/
